pragma solidity ^0.8.0;

contract FirstContract{
    // variables types in solidity
    // 1. Fixed size types

    uint a;
    bool isBool; true false
    address metoyou;
    bytes32 b;

    // variables size types 
    
    string myName;
    bytes b;
    uint[] arr;
    mapping ;

    // 3. user define data types

    struct car {
        string brandName:
        uint price;
    }

    enum Color{
        Blue,
        red,
        White
    }
    color.Blue
}
